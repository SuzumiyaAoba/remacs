;;; frame.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's frame.el is folded into the built-in prelude; this
;; stub only satisfies (require 'frame) forms in ported libraries.

(provide (quote frame))

;;; frame.el ends here
