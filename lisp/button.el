;;; button.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's button.el is folded into the built-in prelude; this
;; stub only satisfies (require 'button) forms in ported libraries.

(provide (quote button))

;;; button.el ends here
