;;; custom.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's custom.el is folded into the built-in prelude; this
;; stub only satisfies (require 'custom) forms in ported libraries.

(provide (quote custom))

;;; custom.el ends here
