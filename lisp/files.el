;;; files.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's files.el is folded into the built-in prelude; this
;; stub only satisfies (require 'files) forms in ported libraries.

(provide (quote files))

;;; files.el ends here
