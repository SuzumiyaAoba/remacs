;;; mwheel.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's mwheel.el is folded into the built-in prelude; this
;; stub only satisfies (require 'mwheel) forms in ported libraries.

(provide (quote mwheel))

;;; mwheel.el ends here
