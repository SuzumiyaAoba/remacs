;;; faces.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's faces.el is folded into the built-in prelude; this
;; stub only satisfies (require 'faces) forms in ported libraries.

(provide (quote faces))

;;; faces.el ends here
