;;; simple.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's simple.el is folded into the built-in prelude; this
;; stub only satisfies (require 'simple) forms in ported libraries.

(provide (quote simple))

;;; simple.el ends here
