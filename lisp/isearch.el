;;; isearch.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's isearch.el is folded into the built-in prelude; this
;; stub only satisfies (require 'isearch) forms in ported libraries.

(provide (quote isearch))

;;; isearch.el ends here
