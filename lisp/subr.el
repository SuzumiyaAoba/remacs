;;; subr.el --- provided by the remacs prelude -*- lexical-binding: t -*-

;; remacs: GNU's subr.el is folded into the built-in prelude; this
;; stub only satisfies (require 'subr) forms in ported libraries.

(provide (quote subr))

;;; subr.el ends here
